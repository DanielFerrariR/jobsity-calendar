import { DataCredentials } from 'src/store/credentials'

const dataCredentials: DataCredentials = {
  credential_password: 'Password',
  description: 'Description',
  prevent_clipboard: true,
  shortcut_name: 'Name',
  user_parameters: {
    user: 'Daniel'
  }
}

export default dataCredentials
