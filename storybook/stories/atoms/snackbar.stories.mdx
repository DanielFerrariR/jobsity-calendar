import { useState } from 'react'
import { Snackbar, IconButton, Button } from 'src/components/atoms'
import { Check, Close } from '@material-ui/icons'
import { Meta, Story, Preview } from '@storybook/addon-docs/blocks'

<Meta title="Components/atoms/Snackbar" component={Snackbar} />

# Snackbar

This component was created from Snackbar of [@material-ui](https://material-ui.com).

[Snackbar API](https://material-ui.com/api/snackbar)

<Preview>
  <Story name="SnackBar">
    {() => {
      const [open, setOpen] = useState(false)
      return (
        <>
          <Button onClick={() => setOpen(true)}> Open SnackBar </Button>
          <Snackbar
            open={open}
            onClose={() => setOpen(false)}
            message="This is snackbar"
            action={
              <>
                <IconButton
                  color="common.white"
                  size="small"
                  onClick={() => setOpen(false)}
                >
                  <Check fontSize="small" />
                </IconButton>
                <IconButton
                  color="common.white"
                  size="small"
                  onClick={() => setOpen(false)}
                >
                  <Close fontSize="small" />
                </IconButton>
              </>
            }
          />
        </>
      )
    }}
  </Story>
</Preview>
