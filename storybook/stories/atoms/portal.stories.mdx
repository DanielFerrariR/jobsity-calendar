import { useState } from 'react'
import { Portal, Box, Typography } from 'src/components/atoms'
import { Meta, Story, Preview } from '@storybook/addon-docs/blocks'

<Meta title="Components/atoms/Portal" component={Portal} />

# Portal

This component was created from Portal of [@material-ui](https://material-ui.com).

[Portal API](https://material-ui.com/api/portal)

<Preview>
  <Story name="portal">
    {() => {
      const [portalRef, setPortalRef] = useState(null)
      return (
        <>
          <Box>
            <Typography>It looks like I will render here.</Typography>
            <Portal container={portalRef}>
              <Typography>But I actually render here!</Typography>
            </Portal>
          </Box>
          <Box ref={(newRef) => setPortalRef(newRef)} width={1} />
        </>
      )
    }}
  </Story>
</Preview>
